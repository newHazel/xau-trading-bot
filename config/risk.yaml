# F10: account size used to size the suggested lot in alerts. Set this to YOUR real
# balance so the lot_size shown is correct for your account (alerts only — no auto-trade).
account_balance: 10000.0
# Loosen-the-brakes EXPERIMENT (2026-06-12): the server backtest on Apr10→Jun12 showed
# no_killzone earning its keep — 67 sig (vs 22 baseline), PF 1.57 vs 1.27, +17.8R vs
# +2.3R. Bypassing the kill-zone gate lets the bot trade ANY session. The 2-month
# sample is noisy (baseline freshness PF varied 3.13↔1.27 across periods), so this is
# a deliberate forward-data bet: the outcome tracker + near-miss logger will measure
# real forward results. Flip back to false to restore the kill-zone-only behavior.
ignore_kill_zone: true
risk_per_trade_percent: 0.5
max_risk_per_trade_percent: 1.0
max_daily_losses: 2
max_daily_trades: 3
stop_after_tp: true
rr_tiers:
  min_to_enter: 2.0
  required_for_grade_b: 1.5
  required_for_grade_a: 2.0
  required_for_grade_a_plus: 2.5
tp1_r: 2.0
tp2_r: 3.5
require_liquidity_target_for_tp2: true
atr_period: 14
atr_sl_multiplier: 1.5
swing_buffer_atr_ratio: 0.20
sl_invalidation_mode: "min_of_sweep_and_fvg"  # sweep_low / fvg_bottom / min_of_sweep_and_fvg / swing_low
sl_buffer_atr_ratio: 0.20
re_entry:
  allow_after_sl: false
  allow_after_tp: false
  require_new_setup_id: true
  require_new_sweep: true
  cooldown_minutes_after_loss: 60
